import { InfoCardDirective } from './info-card.directive';

describe('InfoCardDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoCardDirective();
    expect(directive).toBeTruthy();
  });
});
